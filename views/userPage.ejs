
<% include ./partials/header %>

  <div class="ui doubling stackable container">
   <div class="ui top  segment">

  <div class="ui raised centered segment">
    <h1 class="ui huge teal header">Hello there! <span class="hiUser"> <%= (user.username)[0].toUpperCase() + (user.username).substring(1) %> </span> </h1>
    <h2 class="ui large header"> Nice to see you!</h2>
    <p class="text">Start creating your collection.</p>
      
      <form action="/jokes/users/new/<%= user._id %>" method="GET">
          <button class="ui purple basic big button">Add a new joke</button>
      </form>   
   </div>

      <div class="ui two stackable cards">

    <% jokes.forEach(function(joke) { %>
     
      <section class="ui padded purple  card" role="region">

      <div class="content">
        <h2 class="header"> <%= joke.title %></h2>
      </div>

      <%if ( joke.image  !== '') { %>
          
          <div class="ui medium centered image" id="image">
            <img class="ui centered rounded image" src="<%= joke.image %>" alt="joke image">
          </div>
      
      <% } %>

          <div class="content">
          
            <div class="meta">
              <span class="date">Added on <%= joke.publishDate %></span>
            </div>
            <div class="description">
              <p class="joke-content"><%= joke.content %></p>
            </div>
            <div class="extra content btns">
              <a class="ui orange basic big button" role="button" href="/jokes/users/edit/<%= joke._id %>">Edit</a>
              <form id="btn-delete" action="/jokes/users/<%= joke._id %>?_method=DELETE" method="POST">
                <button class="ui black basic big button">Delete</button>
              </form>
            </div>
        </div>
      </section>
      
    <% }); %>

    </div>

    </div>
  </div>

<div class="margin-top"></div>

</main>
</body>
</html>
